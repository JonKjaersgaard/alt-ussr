package ussr.samples.atron.framework.annotations;

/**
 * Annotation indicating a method that should run once whenever the role is activated (either when
 * a role is first assigned or after a role change).
 * @author ups
 */
public @interface Startup {
}
